#!/usr/bin/env bash
# Lanza el gestor de ficheros fang del entorno devbox de este repositorio.
set -euo pipefail
REPO_DIR="$(cd "$(dirname "$(readlink "${BASH_SOURCE[0]}" || echo "${BASH_SOURCE[0]}")")/.." && pwd)"
exec devbox run -q -c "$REPO_DIR" -- fang "$@"
